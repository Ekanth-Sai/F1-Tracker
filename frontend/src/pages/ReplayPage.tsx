import { Box, Typography } from "@mui/material";

export default function ReplayPage() {
    return (
        <Box p={3}>
            <Typography variant="h1">Replay Page</Typography>
        </Box>
    );
}