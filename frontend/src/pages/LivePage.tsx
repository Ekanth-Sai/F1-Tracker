import { Box, Typography } from "@mui/material";

export default function LivePage() {
    return (
        <Box p={3}>
            <Typography variant="h1">Live Tracking</Typography>
        </Box>
    );
}